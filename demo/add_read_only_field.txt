

I have a task:

Add a read-only field (named is_read_only) for negotiations
How read only mode is set:

we have un-merged changes  -> true

(and all other states (i.e., accepted, agreed, verified, finalized, terminated) except requested and offered) -> true





we have un-merge changes and state is requested or offered:

1. On GUI, Click "Send" to submit a new offer/request
2. to call create_policy function make a payload, to call api (/provider/offer/new or /consumer/request/new) to create a new policy document and save into mongodb
3. get this policy doc and compared with previous policy by find_changes function if changes available, set is_read_only = True
4. updating the current policy with is_read_only = True


for others states, accepted, agreed, offered, verified, finalized, terminated, set the is_read_only = True in the last policy




